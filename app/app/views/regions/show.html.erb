<p id="notice"><%= notice %></p>

<form>
  <div class="mb-3">
    <%= label_tag nil, 'DDD' %>
    <%= text_field_tag nil, @region.ddd, class:"form-control", disabled: true %>
  </div>

  <div class="mb-3">
    <%= label_tag nil, 'Nome' %>
    <%= text_field_tag nil, @region.name, class:"form-control", disabled: true %>
  </div>
</form>
<br>
<%= link_to 'Editar', edit_region_path(@region), class:"btn btn-primary" %> |
<%= link_to 'Voltar', regions_path, class:"btn btn-primary" %>
